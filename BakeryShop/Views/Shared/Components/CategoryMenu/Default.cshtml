@model IEnumerable<Category>

<li class="dropdown">
    <a asp-controller="Bread"
       asp-action="List"
       class="dropdown-toggle" data-toggle="dropdown">Breads<b class="caret"></b></a>
    <ul class="dropdown-menu">
        @foreach (var category in Model)
        {
            <li>
                <a asp-controller="Bread" asp-action="List"
                   asp-route-category="@category.CategoryName">@category.CategoryName</a>
            </li>
        }
        <li class="divider"></li>
        <li>
            <a asp-controller="Bread" asp-action="List" asp-route-Category="">Vezi toate produsele</a>
        </li>
    </ul>
</li>